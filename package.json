{
  "name": "visor-v2",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build:main",
    "build:main": "npx terser src/main.js --compress --mangle --output dist/main.js"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1",
    "terser": "^5.39.0",
    "wrangler": "^4.6.0"
  }
}
